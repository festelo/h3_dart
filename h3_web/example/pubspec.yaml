name: h3_web_example
description: An absolute bare-bones web app.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  h3_web:
    path: ..
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.5
  lints: ^6.0.0
  webdev: ^3.7.1

dependency_overrides:
  h3_common:
    path: ../../h3_common
