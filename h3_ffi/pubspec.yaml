name: h3_ffi
description: The package provides Dart version of the H3 Core library, a hexagon-based geographic grid system
version: 0.6.2
homepage: https://github.com/festelo/h3_dart
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  ffi: ^2.0.1
  path: ^1.8.1
  h3_common: ^0.7.0

dev_dependencies:
  ffigen: ^19.0.0
  collection: # uses workspace's version
  test: # uses workspace's version
